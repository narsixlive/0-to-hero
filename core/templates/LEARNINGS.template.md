# Learnings — [Workspace name]

<!-- Workspace rule register (Durcir lifecycle). Kept separate from CONTEXT.md so the
     situational context stays pure and the rule lifecycle has its own surface.
     Ladder: L1 Active (here, injected) → L2 AGENT.md `## Rules` (role doctrine)
             → L3 root CLAUDE.md `## Gotchas` (cross-workspace doctrine).
     Only `## Active Learnings` is auto-injected at SessionStart. -->

## Active Learnings
<!-- In-flight workspace rules, injected every session. Keep lean (~5). -->
<!-- Format: `- ALWAYS/NEVER [action] ([why]) ×N`  — N = times this rule recurred. -->
<!-- /memorise BUMPS ×N on recurrence (never silently skips a duplicate — the duplicate IS the signal). -->
<!-- Graduate at ×3 (or cross-workspace / high-criticality): copy the rule verbatim (with its why)
     into AGENT.md `## Rules` (this workspace) or via /gotcha into root CLAUDE.md `## Gotchas`,
     then REMOVE it from here. The list drains upward; it never just grows. -->

## Archived Learnings
<!-- Demoted or non-recurrent rules. Kept for reference, NOT injected. -->
<!-- /memorise rescans this section when matching a new candidate (catches a déjà-vu rule). -->
<!-- Promote back to Active Learnings when a relevant scenario re-emerges. Never delete to "lighten" — only sort. -->

## Drift log
<!-- Audit trail: one line per rule that was ignored despite being capitalized. NOT injected. -->
<!-- Format: `- [rule] | captured YYYY-MM-DD | drift YYYY-MM-DD | delay Nd | L?→L? | note` -->
<!-- A rule that drifts < 7 days systematically is in the wrong layer → graduate it straight to L3. -->
